# Android Toolchain Direct Fallback

The Android release builder uses an ordered set of official Google repository endpoints for direct package provisioning when sdkmanager cannot retrieve repository manifests.
