/// <reference types="node" />
import { execFileSync } from "child_process";
import { existsSync, readFileSync } from "node:fs";
import { join, normalize, relative, resolve, sep } from "node:path";
import { ConstructionContext, ConstructionTool } from "../../Builder/Autonomous/AutonomousConstructionEngine";
import { ensurePytest } from "./PythonVerificationBootstrap";
import { CommercialArtifactQualityAudit } from "./CommercialArtifactQualityAudit";

function run(command:string,args:string[],cwd:string,timeout=15*60*1000){const started=Date.now();try{let executable=command;let executableArgs=args;if(process.platform==="win32"){if(command==="git")executable="git.exe";if(command==="npx"){executable=process.env.ComSpec||"cmd.exe";executableArgs=["/d","/s","/c","npx.cmd",...args];}}const output=execFileSync(executable,executableArgs,{cwd,encoding:"utf8",timeout,shell:false,windowsHide:true,stdio:["ignore","pipe","pipe"]});return{ok:true,code:0,output:String(output),error:null,elapsedMs:Date.now()-started};}catch(error:any){const stdout=String(error?.stdout||"");const stderr=String(error?.stderr||"");const exitCode=error?.status??1;const expectedGitDiffQuiet=command==="git"&&args[0]==="diff"&&args[1]==="--cached"&&args[2]==="--quiet"&&exitCode===1;if(expectedGitDiffQuiet)return{ok:true,code:1,output:`${stdout}${stderr}`,error:null,elapsedMs:Date.now()-started};return{ok:false,code:exitCode,output:`${stdout}\n${stderr}`,error:error?.message??`${command} failed`,elapsedMs:Date.now()-started};}}
function commandExists(command:string,cwd:string){try{const locator=process.platform==="win32"?"where.exe":"which";execFileSync(locator,[command],{cwd,encoding:"utf8",windowsHide:true,stdio:["ignore","pipe","ignore"]});return true;}catch{return false;}}
export type ImplementationAgent="python";
export function resolveImplementationAgent(root=process.cwd()):ImplementationAgent|null{const requested=process.env.HOOSHYAR_AGENT?.trim().toLowerCase();if(requested&&requested!=="python")return null;return commandExists("python",root)?"python":null;}
export function repositoryStateChanged(before:string,after:string){return before.trim()!==after.trim();}
export function buildAgentArgs(_agent:ImplementationAgent,prompt:string){return["Backend/AI_Runtime/autonomous_builder.py","--prompt",prompt];}
const DEFAULT_DIRECTIVES=["Implement exactly one concrete capability from the canonical mission.","Create or update the focused implementation, focused test and documentation required by the architecture.","Run focused verification for the selected knot; run Autonomous Builder tests periodically; run the full Jest suite only at the periodic integration checkpoint.","Repair verification failures before finalization.","Do not redesign Architecture Freeze V4.","Never modify an existing dependency, engine, test or document merely to make the selected capability appear implemented.","For a product capability, implement the product artifact paths declared by the durable product roadmap; do not substitute the target engine as the implementation artifact."];
const REPOSITORY_STATUS_ARGS=["status","--porcelain=v1","--untracked-files=all","--",".",":(exclude)node_modules"];
const FULL_VERIFY_EVERY=Math.max(1,Number.parseInt(process.env.HOOSHYAR_FULL_VERIFY_EVERY||"10",10)||10);const BUILDER_TEST_EVERY=Math.max(1,Number.parseInt(process.env.HOOSHYAR_BUILDER_TEST_EVERY||"5",10)||5);
function focusedTestFor(capabilityId:string,root=process.cwd()):string|null{const canonicalCapabilityId=capabilityId.replace(/^(repair-)+/,"");const roadmapPath=join(root,"Docs","Product","PRODUCT_CONSTRUCTION_ROADMAP.json");if(existsSync(roadmapPath)){try{const roadmap=JSON.parse(readFileSync(roadmapPath,"utf8")) as{capabilities?:Array<{capabilityId?:string;testPath?:string}>};const capability=roadmap.capabilities?.find(item=>item.capabilityId===canonicalCapabilityId);if(capability?.testPath)return capability.testPath;}catch{}}return null;}
export function productRoadmapPaths(root:string,capabilityId:string):string[]{const roadmapPath=join(root,"Docs","Product","PRODUCT_CONSTRUCTION_ROADMAP.json");if(!existsSync(roadmapPath))return[];const canonicalCapabilityId=capabilityId.replace(/^(repair-)+/,"");try{const roadmap=JSON.parse(readFileSync(roadmapPath,"utf8")) as{capabilities?:Array<{capabilityId?:string;implementationPath?:string;testPath?:string;documentationPath?:string}>};const capability=roadmap.capabilities?.find(item=>item.capabilityId===canonicalCapabilityId);if(!capability)return[];return[capability.implementationPath,capability.testPath,capability.documentationPath].filter(Boolean).map(path=>join(root,path!));}catch{return[];}}
function extractRepairScopeCapabilityId(capabilityId:string,capabilityText:string):string|null{const explicit=capabilityText.match(/repair(?: and re-verify)? knot (?:repair-)?([a-z0-9._-]+)/i);if(explicit)return explicit[1].startsWith("product.")?explicit[1]:`product.${explicit[1]}`;const embedded=capabilityText.match(/repair-(product\.[a-z0-9._-]+)/i);if(embedded)return embedded[1];return capabilityId.startsWith("repair-product.")?capabilityId.replace(/^repair-/i,""):null;}
export function declaredArtifactPaths(root:string,capabilityId:string,targetEngine:string,capabilityText=""):string[]{const canonical=capabilityId.replace(/^(repair-)+/,"");const scope=extractRepairScopeCapabilityId(capabilityId,capabilityText);if(canonical==="typescript-contract-integrity"&&scope){const scoped=productRoadmapPaths(root,scope);if(scoped.length>0)return scoped;}const roadmapPaths=productRoadmapPaths(root,canonical);if(roadmapPaths.length>0)return roadmapPaths;return[join(root,"Backend","HBOS","Engines",`${targetEngine.replace(/[^A-Za-z0-9]/g,"")}.ts`),join(root,"Backend","HBOS","test",`${targetEngine.replace(/[^A-Za-z0-9]/g,"")}.test.ts`),join(root,"Docs","Engines",`${targetEngine.replace(/[^A-Za-z0-9]/g,"")}.md`)];}
function relativeStatusPaths(statusOutput:string,root:string){return statusOutput.split(/\r?\n/).map(line=>line.slice(3).trim()).filter(Boolean).map(path=>normalize(join(root,path)));}
export function isDeclaredArtifactPath(candidate:string,declared:string){const candidateAbs=resolve(candidate);const declaredAbs=resolve(declared);if(candidateAbs===declaredAbs)return true;if(!existsSync(declaredAbs))return false;const child=relative(declaredAbs,candidateAbs);return child!==""&&child!==".."&&!child.startsWith(`..${sep}`)&&!child.split(sep).includes("..");}
export function unexpectedArtifactPaths(changedPaths:string[],allowedPaths:string[]){return changedPaths.filter(candidate=>!allowedPaths.some(declared=>isDeclaredArtifactPath(candidate,declared)));}
export function evaluateBehavioralEvidence(root:string,capabilityId:string,jestPassed:boolean,jestOutput:string){const canonicalCapabilityId=capabilityId.replace(/^(repair-)+/,"");if(canonicalCapabilityId.startsWith("product.")){const paths=productRoadmapPaths(root,canonicalCapabilityId);if(paths.length>=2){const quality=new CommercialArtifactQualityAudit().auditCapability(root,canonicalCapabilityId,paths[0].slice(root.length+1),paths[1].slice(root.length+1));return{verified:jestPassed&&quality.complete,source:"commercial-artifact-quality" as const,failures:quality.failures};}}return{verified:jestPassed&&/(?:behavior|analy|calculat|evaluat|assess|transform|ingest|persist|authorize|decision|kpi|financial|variance|evidence|ready)/i.test(jestOutput),source:"test-output" as const,failures:[]};}
function buildAgentPrompt(context:ConstructionContext){const requiredPaths=declaredArtifactPaths(process.cwd(),context.plan.capabilityId,context.plan.targetEngine,context.plan.capability);return["You are the repository-native Python implementation worker inside HooshyarOS Autonomous Operations Engine.","Read AGENTS.md, Docs/ARCHITECTURE.md, and Assistant/SYSTEM_PROMPT.md before changing code.","The frozen platform architecture, its Engines, decision logic, lifecycle, governance and autonomous construction rules are authoritative inputs to this mission.","Architecture Freeze V4 is authoritative; do not redesign or duplicate existing engines.","Implement exactly ONE capability for this mission:",`Capability ID: ${context.plan.capabilityId}`,`Capability: ${context.plan.capability}`,`Target Engine: ${context.plan.targetEngine}`,`Dependencies: ${context.plan.dependencies.join(", ")||"none"}`,`Required artifact paths: ${requiredPaths.join(" ; ")}`,`Architecture rules: ${context.plan.architectureRules.join(" ; ")||"preserve existing rules"}`,`Directives: ${DEFAULT_DIRECTIVES.join(" ; ")}`,"For product capabilities, the required artifact paths above are the authoritative product boundary. Do not implement the capability by rewriting an existing engine unless one of those paths is that engine path.","Use only repository-native Python construction. Do not invoke Copilot, Codex, Claude, or any cloud coding CLI.","Reuse existing capabilities and engine boundaries; never invent business semantics that are absent from repository architecture or evidence.","Produce a real repository change when the selected deterministic capability is missing."] .join("\n");}
export function createLocalConstructionTools(root=process.cwd()):ConstructionTool[]{let verificationCount=0;let builderTestsRequired=false;return[
{name:"architecture",execute:(_stage,context)=>({ok:Boolean(context.plan.capabilityId&&context.plan.capability&&context.plan.targetEngine),artifact:{approved:true,capabilityId:context.plan.capabilityId,targetEngine:context.plan.targetEngine,architectureRules:context.plan.architectureRules,directives:DEFAULT_DIRECTIVES,requiredPaths:declaredArtifactPaths(root,context.plan.capabilityId,context.plan.targetEngine,context.plan.capability)}})},
{name:"generator",execute:(stage,context)=>{if(stage!=="GENERATE")return{ok:true};const before=run("git",REPOSITORY_STATUS_ARGS,root);if(!before.ok)return{ok:false,issue:"AUTONOMOUS_REPOSITORY_STATE_UNAVAILABLE",artifact:before};if(before.output.trim())return{ok:false,issue:"AUTONOMOUS_WORKTREE_DIRTY",artifact:{clean:false,output:before.output}};const agent=resolveImplementationAgent(root);if(!agent)return{ok:false,issue:"AUTONOMOUS_AGENT_UNAVAILABLE",artifact:{provider:null,changed:false}};const requiredPaths=declaredArtifactPaths(root,context.plan.capabilityId,context.plan.targetEngine,context.plan.capability);const result=run(agent,buildAgentArgs(agent,buildAgentPrompt(context)),root,30*60*1000);const after=run("git",REPOSITORY_STATUS_ARGS,root);const changed=after.ok&&repositoryStateChanged(before.output,after.output);const changedPaths=after.ok?relativeStatusPaths(after.output,root):[];const unexpectedPaths=unexpectedArtifactPaths(changedPaths,requiredPaths);const touchesDeclaredArtifact=changedPaths.some(path=>requiredPaths.some(declared=>isDeclaredArtifactPath(path,declared)));const artifact={idempotent:false,type:"AUTONOMOUS_AGENT_GENERATION_RESULT",provider:agent,capabilityId:context.plan.capabilityId,capability:context.plan.capability,targetEngine:context.plan.targetEngine,requiredPaths,changedPaths,unexpectedPaths,exitCode:result.code,changed,elapsedMs:result.elapsedMs,output:result.output,error:result.error,timestamp:new Date().toISOString()};console.log(JSON.stringify(artifact,null,2));if(!result.ok)return{ok:false,issue:"AUTONOMOUS_AGENT_GENERATION_FAILED",artifact};if(!after.ok)return{ok:false,issue:"AUTONOMOUS_REPOSITORY_STATE_UNAVAILABLE",artifact};const idempotent=Boolean(!changed&&requiredPaths.every(path=>existsSync(path))&&unexpectedPaths.length===0);artifact.idempotent=idempotent;if(!changed&&!idempotent)return{ok:false,issue:"AUTONOMOUS_AGENT_NO_REPOSITORY_CHANGE",artifact};if(!touchesDeclaredArtifact&&!idempotent||unexpectedPaths.length>0)return{ok:false,issue:"AUTONOMOUS_ARTIFACT_BOUNDARY_VIOLATION",artifact};return{ok:true,artifact};}},
{name:"python",execute:(stage,context)=>{if(stage!=="VERIFY")return{ok:true};verificationCount++;const focused=focusedTestFor(context.plan.capabilityId,root);const fullVerify=verificationCount%FULL_VERIFY_EVERY===0||process.env.HOOSHYAR_FULL_VERIFY==="1";const runBuilderTests=builderTestsRequired||verificationCount%BUILDER_TEST_EVERY===0||fullVerify||process.env.HOOSHYAR_BUILDER_VERIFY==="1";const syntax=run("python",["-m","compileall","-q","Backend/AI_Runtime"],root);if(!syntax.ok)return{ok:false,issue:"AUTONOMOUS_PYTHON_SYNTAX_VERIFY_FAILED",artifact:{syntaxVerified:false,elapsedMs:syntax.elapsedMs,output:syntax.output,error:syntax.error}};let bootstrap={ok:true,installed:false,output:"pytest verification skipped",error:null as string|null};let builderTests={ok:true,code:0,output:"builder verification skipped",error:null as string|null,elapsedMs:0};if(runBuilderTests){bootstrap=ensurePytest(root);if(!bootstrap.ok){builderTestsRequired=true;return{ok:false,issue:"AUTONOMOUS_PYTEST_BOOTSTRAP_FAILED",artifact:{syntaxVerified:true,pytestBootstrapped:false,syntaxElapsedMs:syntax.elapsedMs,output:bootstrap.output,error:bootstrap.error}};}builderTests=run("python",["-m","pytest","Backend/AI_Runtime/tests/test_autonomous_builder_platform.py","Backend/AI_Runtime/tests/test_autonomous_spec.py","-q"],root);if(!builderTests.ok){builderTestsRequired=true;return{ok:false,issue:"AUTONOMOUS_BUILDER_TESTS_FAILED",artifact:{syntaxVerified:true,pytestBootstrapped:true,builderTestsPassed:false,output:builderTests.output,error:builderTests.error}};}builderTestsRequired=false;}if(!focused)return{ok:true,artifact:{syntaxVerified:true,builderTestsPassed:builderTests.ok,focusedTest:null,evidence:"no-focused-test-mapped"}};const focusedResult=run("npx",["jest","--runInBand",focused],root,20*60*1000);const behavioral=evaluateBehavioralEvidence(root,context.plan.capabilityId,focusedResult.ok,focusedResult.output);const fullResult=fullVerify?run("npx",["jest","--runInBand"],root,45*60*1000):{ok:true,output:"full verification not scheduled",code:0,error:null,elapsedMs:0};const currentStatus=run("git",REPOSITORY_STATUS_ARGS,root);const changedPaths=currentStatus.ok?relativeStatusPaths(currentStatus.output,root):[];const allowedPaths=declaredArtifactPaths(root,context.plan.capabilityId,context.plan.targetEngine,context.plan.capability);const generatorArtifact=context.artifacts.GENERATE as{changedPaths?:string[];unexpectedPaths?:string[]}|undefined;const generatorChangedPaths=generatorArtifact?.changedPaths??[];const unexpectedCurrentPaths=unexpectedArtifactPaths(changedPaths,allowedPaths);const unexpectedGeneratorPaths=unexpectedArtifactPaths(generatorChangedPaths,allowedPaths);const generatorIdempotent=Boolean((generatorArtifact as any)?.idempotent);const repositoryEvidenceValid=currentStatus.ok&&unexpectedCurrentPaths.length===0&&unexpectedGeneratorPaths.length===0&&(generatorChangedPaths.length>0||generatorIdempotent);const artifact={syntaxVerified:true,builderTestsPassed:builderTests.ok,focusedTest:focused,focusedTestPassed:focusedResult.ok,behavioralEvidenceVerified:behavioral.verified,behavioralEvidenceSource:behavioral.source,behavioralEvidenceFailures:behavioral.failures,integrationVerified:fullResult.ok,cleanRepository:repositoryEvidenceValid,repositoryEvidenceMode:"declared-artifacts-only-until-finalize",changedPaths,unexpectedCurrentPaths,unexpectedGeneratorPaths,jestVerified:focusedResult.ok&&fullResult.ok,testsPassed:focusedResult.ok&&fullResult.ok,output:`${focusedResult.output}\n${fullResult.output}`};if(!focusedResult.ok)return{ok:false,issue:"AUTONOMOUS_FOCUSED_TEST_FAILED",artifact};if(!behavioral.verified)return{ok:false,issue:"AUTONOMOUS_BEHAVIORAL_EVIDENCE_INCOMPLETE",artifact};if(!fullResult.ok)return{ok:false,issue:"AUTONOMOUS_FULL_INTEGRATION_TEST_FAILED",artifact};if(!repositoryEvidenceValid)return{ok:false,issue:"AUTONOMOUS_WORKTREE_DIRTY_AFTER_VERIFY",artifact};return{ok:true,artifact};}},
{name:"git",execute:(stage,context)=>{if(stage!=="FINALIZE")return{ok:true};const status=run("git",REPOSITORY_STATUS_ARGS,root);if(!status.ok)return{ok:false,issue:"GIT_STATUS_FAILED",artifact:status};if(!status.output.trim())return{ok:false,issue:"GIT_NO_REPOSITORY_CHANGE",artifact:{clean:true,changed:false,output:status.output}};const added=run("git",["add","-A"],root);if(!added.ok)return{ok:false,issue:"GIT_ADD_FAILED",artifact:added};const diff=run("git",["diff","--cached","--quiet"],root);if(!diff.ok&&diff.code!==1)return{ok:false,issue:"GIT_STAGED_DIFF_CHECK_FAILED",artifact:diff};if(diff.code===0)return{ok:false,issue:"GIT_NO_REPOSITORY_CHANGE",artifact:{clean:true,changed:false,output:status.output}};const message=`feat(hbos): autonomous construction ${context.plan.capabilityId}`;const commit=run("git",["commit","-m",message],root);if(!commit.ok)return{ok:false,issue:"GIT_COMMIT_FAILED",artifact:commit};const branch=run("git",["branch","--show-current"],root);if(!branch.ok||!branch.output.trim())return{ok:false,issue:"GIT_BRANCH_UNAVAILABLE",artifact:branch};const push=run("git",["push","origin",branch.output.trim()],root);if(!push.ok)return{ok:false,issue:"GIT_PUSH_FAILED",artifact:push};return{ok:true,artifact:{committed:true,pushed:true,message,output:`${commit.output}\n${push.output}`}};}}
];}