This marker is intentionally documentation-only and must not be treated as a completion signal by runtime code.

The authoritative completion condition is executable verification of the autonomous continuation lifecycle and an empty genuinely-missing capability backlog.
